---
# maintains mainnet inventory grouped by project names
all:
  vars:
    env: mainnet
  children:
    ika:
      hosts:
        validator.ika.mainnet.encapsulate.xyz:
        64.20.36.102:
          type: validator
